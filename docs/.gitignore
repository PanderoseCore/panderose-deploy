# Dependencies
/node_modules

# Production
/build
/dist

# Generated from specs/ on every build via `npm run gen-api-docs:all` —
# never hand-edited, so don't commit it (avoids stale/duplicated diffs).
content/public/api/
content/internal/api/

# Generated files
.docusaurus
.cache-loader

# Misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
