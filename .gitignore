# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.swp
.vscode/
.idea/

# Node / tooling (docs/ uses Node; the root site itself still doesn't)
node_modules/
npm-debug.log*

# Build output — assembled by scripts/package-site.sh / CI, never committed
/dist

# Local env / secrets — never commit
.env
.env.*
*.pem
